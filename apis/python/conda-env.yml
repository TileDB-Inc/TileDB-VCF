name: tiledbvcf-py
channels:
  - conda-forge
dependencies:
  - numpy
  - python<3.12 # based on available tiledb-py wheels
  - pybind11
  - pyarrow
  - fsspec
  - dask
  - pip
  - setuptools
  - setuptools_scm
  - setuptools_scm_git_archive
  - pip:
    - pytest
    - pytest-runner
